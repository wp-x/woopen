联通云盘 API 参数文档
1. 认证参数
必需参数
refresh_token (string, 必需): 刷新令牌，用于获取访问令牌
access_token (string, 可选): 访问令牌，系统会自动刷新
认证流程
// 初始化客户端  
d.client = wopan.DefaultWithRefreshToken(d.RefreshToken)  
d.client.SetAccessToken(d.AccessToken)  
  
// 自动刷新令牌回调  
d.client.OnRefreshToken(func(accessToken, refreshToken string) {  
    d.AccessToken = accessToken  
    d.RefreshToken = refreshToken  
})
driver.go:31-38

2. 存储空间参数
family_id (string, 可选): 家庭云ID，留空则使用个人云盘
defaultFamilyID (string): 系统自动获取的默认家庭ID
// 获取家庭信息  
fml, err := d.client.FamilyUserCurrentEncode()  
d.defaultFamilyID = strconv.Itoa(fml.DefaultHomeId)
driver.go:39-44

3. 文件操作API
3.1 文件列表 (QueryAllFiles)
data, err := d.client.QueryAllFiles(  
    spaceType,    // 空间类型  
    dirID,        // 目录ID  
    pageNum,      // 页码  
    pageSize,     // 每页大小  
    0,            // 排序方式  
    familyID,     // 家庭ID  
    callback      // 请求回调  
)
driver.go:56-58

3.2 获取下载链接 (GetDownloadUrlV2)
res, err := d.client.GetDownloadUrlV2(  
    []string{fileID},  // 文件ID列表  
    callback           // 请求回调  
)  
// 返回: res.List[0].DownloadUrl
driver.go:77-85

3.3 创建目录 (CreateDirectory)
_, err := d.client.CreateDirectory(  
    spaceType,    // 空间类型  
    parentID,     // 父目录ID  
    dirName,      // 目录名称  
    familyID,     // 家庭ID  
    callback      // 请求回调  
)
driver.go:95-98

3.4 文件上传 (Upload2C)
_, err := d.client.Upload2C(  
    spaceType,    // 空间类型  
    wopan.Upload2CFile{  
        Name:        fileName,        // 文件名  
        Size:        fileSize,        // 文件大小  
        Content:     fileStream,      // 文件流  
        ContentType: mimeType,        // MIME类型  
    },  
    parentID,      // 父目录ID  
    familyID,      // 家庭ID  
    wopan.Upload2COption{  
        OnProgress: progressCallback,  // 进度回调  
        Ctx:        context,          // 上下文  
    }  
)
driver.go:155-166

3.5 文件移动 (MoveFile)
err := d.client.MoveFile(  
    dirList,      // 目录ID列表  
    fileList,     // 文件ID列表  
    dstDirID,     // 目标目录ID  
    srcSpaceType, // 源空间类型  
    dstSpaceType, // 目标空间类型  
    srcFamilyID,  // 源家庭ID  
    dstFamilyID,  // 目标家庭ID  
    callback      // 请求回调  
)
driver.go:109-113

3.6 文件重命名 (RenameFileOrDirectory)
err := d.client.RenameFileOrDirectory(  
    spaceType,    // 空间类型  
    fileType,     // 文件类型 (0:目录, 1:文件)  
    fileID,       // 文件/目录ID  
    newName,      // 新名称  
    familyID,     // 家庭ID  
    callback      // 请求回调  
)
driver.go:121-123

3.7 文件复制 (CopyFile)
err := d.client.CopyFile(  
    dirList,      // 目录ID列表  
    fileList,     // 文件ID列表  
    dstDirID,     // 目标目录ID  
    srcSpaceType, // 源空间类型  
    dstSpaceType, // 目标空间类型  
    srcFamilyID,  // 源家庭ID  
    dstFamilyID,  // 目标家庭ID  
    callback      // 请求回调  
)
driver.go:134-138

3.8 文件删除 (DeleteFile)
err := d.client.DeleteFile(  
    spaceType,    // 空间类型  
    dirList,      // 目录ID列表  
    fileList,     // 文件ID列表  
    callback      // 请求回调  
)
driver.go:149-151

4. 辅助方法
4.1 获取空间类型
func (d *Wopan) getSpaceType() int {  
    if d.FamilyID != "" {  
        return 2 // 家庭云  
    }  
    return 1 // 个人云  
}
4.2 文件对象转换
需要实现 fileToObj 函数将API返回的文件信息转换为内部对象格式。

5. SDK依赖
使用官方SDK: github.com/OpenListTeam/wopan-sdk-go

import "github.com/OpenListTeam/wopan-sdk-go"
6. 完整的独立程序示例结构
package main  
  
import (  
    "context"  
    "github.com/OpenListTeam/wopan-sdk-go"  
)  
  
type WoPanClient struct {  
    client          *wopan.WoClient  
    refreshToken    string  
    accessToken     string  
    familyID        string  
    defaultFamilyID string  
}  
  
func NewWoPanClient(refreshToken string) *WoPanClient {  
    client := wopan.DefaultWithRefreshToken(refreshToken)  
    return &WoPanClient{  
        client:       client,  
        refreshToken: refreshToken,  
    }  
}  
  
func (w *WoPanClient) Init() error {  
    // 设置令牌刷新回调  
    w.client.OnRefreshToken(func(accessToken, refreshToken string) {  
        w.accessToken = accessToken  
        w.refreshToken = refreshToken  
    })  
      
    // 获取家庭信息  
    fml, err := w.client.FamilyUserCurrentEncode()  
    if err != nil {  
        return err  
    }  
    w.defaultFamilyID = strconv.Itoa(fml.DefaultHomeId)  
      
    return w.client.InitData()  
}
Notes
认证机制: 联通云盘使用refresh_token进行认证，系统会自动刷新access_token
空间类型: 1表示个人云，2表示家庭云
错误处理: 所有API调用都需要进行错误检查
上下文支持: 所有操作都支持context.Context用于取消和超时控制
进度回调: 上传操作支持进度回调函数
SDK版本: 使用的是 wopan-sdk-go v0.1.5 版本
基于以上API文档，您可以创建一个独立的轻量网盘程序，实现文件的上传、下载、管理等基本功能。

联通云盘API文档参考引用
以下是我在提供联通云盘API文档时使用的所有参考文件和代码位置：

核心驱动文件
1. 配置参数定义
文件: drivers/wopan/meta.go
内容: 包含WoPan驱动的配置参数结构体定义
关键引用:
参数结构体定义 (第8-17行) meta.go:8-17
驱动配置信息 (第19-23行) meta.go:19-23
驱动注册函数 (第25-29行) meta.go:25-29
2. 主要API实现
文件: drivers/wopan/driver.go
内容: 包含所有文件操作API的具体实现
关键引用:
初始化和认证 (第31-45行) driver.go:31-45
文件列表获取 (第51-73行) driver.go:51-73
下载链接获取 (第75-88行) driver.go:75-88
创建目录 (第90-99行) driver.go:90-99
文件移动 (第101-114行) driver.go:101-114
文件重命名 (第116-124行) driver.go:116-124
文件复制 (第126-139行) driver.go:126-139
文件删除 (第141-152行) driver.go:141-152
文件上传 (第154-167行) driver.go:154-167
3. 辅助方法
文件: drivers/wopan/util.go
内容: 包含排序规则和空间类型等辅助函数
关键引用:
排序规则转换 (第11-28行) util.go:11-28
空间类型判断 (第30-35行) util.go:30-35
系统集成文件
4. 驱动注册
文件: drivers/all.go
内容: 所有驱动的导入列表，包含wopan驱动
关键引用:
WoPan驱动导入 (第80行) all.go:80
5. SDK依赖
文件: go.mod
内容: 项目依赖定义，包含wopan-sdk-go
关键引用:
WoPan SDK依赖 (第14行) go.mod:14
访问方式
您可以通过以下方式访问这些文件：

GitHub仓库: https://github.com/OpenListTeam/OpenList
直接文件链接:
https://github.com/OpenListTeam/OpenList/blob/v4/drivers/wopan/meta.go
https://github.com/OpenListTeam/OpenList/blob/v4/drivers/wopan/driver.go
https://github.com/OpenListTeam/OpenList/blob/v4/drivers/wopan/util.go
https://github.com/OpenListTeam/OpenList/blob/v4/drivers/all.go
https://github.com/OpenListTeam/OpenList/blob/v4/go.mod
相关SDK仓库
WoPan SDK: https://github.com/OpenListTeam/wopan-sdk-go
版本: v0.1.5 (在go.mod中定义) go.mod:14
这些引用涵盖了联通云盘驱动的完整实现，包括参数配置、API调用、错误处理和所有文件操作功能。